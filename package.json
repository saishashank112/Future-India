{
  "name": "future-india-exim-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && node index.js",
    "dev": "npm run dev:client & npm run dev:server",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "react-icons": "^5.6.0"
  }
}
